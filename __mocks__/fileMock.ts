module.exports = {
    src: '/img.svg',
    height: 24,
    width: 24,
    blurDataURL: 'data:image/png;base64,imagedata',
  }

  /**
   * src: uma string que representa o caminho da imagem SVG.
   * height: um número que representa a altura da imagem.
   * width: um número que representa a largura da imagem.
   * blurDataURL: uma string que representa uma imagem em formato base64. Essa imagem é usada como uma versão embaçada da imagem SVG original antes que ela seja totalmente carregada, para melhorar a experiência do usuário.
   */